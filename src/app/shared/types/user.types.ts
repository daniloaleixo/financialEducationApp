import { CHILD, PARENT, WEEKLY, MONTHLY, MALE, FEMALE } from '../constants/barrel-constants';

// ROLE
export enum TRole { CHILD, PARENT };

// FREQUENCE
export enum TAllowanceFrequence { WEEKLY, MONTHLY };

// GENRE
export enum TGenre { MALE, FEMALE };