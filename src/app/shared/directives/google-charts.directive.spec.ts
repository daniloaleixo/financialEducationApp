import { GoogleChartsDirective } from './google-charts.directive';

describe('GoogleChartsDirective', () => {
  it('should create an instance', () => {
    const directive = new GoogleChartsDirective();
    expect(directive).toBeTruthy();
  });
});
